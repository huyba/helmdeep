apiVersion: v1
kind: Service
metadata:
  name: helmdeep-gateway
  labels:
    app.kubernetes.io/name: helmdeep-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: helmdeep-gateway
  ports:
    - name: mcp
      port: 8443
      targetPort: mcp
      protocol: TCP
