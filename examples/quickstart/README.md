# Quickstart

Placeholder. Step 2 will add a `docker run` quickstart here, plus a mock
upstream MCP server so a new user can see a policy allow and a policy deny
within five minutes, without registering any real credentials.
